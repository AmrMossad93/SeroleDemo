import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.altheqav2',
  appName: 'SeroleDemo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
